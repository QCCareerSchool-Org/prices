# Agent Instructions

- When the user asks a question or challenges a design choice, answer the question first. Do not modify files unless the user explicitly asks for a change or clearly says to proceed.
- Treat implementation requests differently from discussion requests. If the user says "show me", "let's try it", "change", "add", "remove", "refactor", or otherwise asks for action, make the requested edits.
